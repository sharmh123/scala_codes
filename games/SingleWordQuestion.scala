package TestCodesScala.games

import TestcodesJava.config.ConfigContainer

class SingleWordQuestion(configFile: String) {
  val configContainer = new ConfigContainer(configFile)
  def generateSingleWordQuestion() = {
  }
}
